spring:
  application:
    name: configserver
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations:
            - file:/Users/adrien/Documents/Informatique/formations/java-microservice/ecom-application/microservice/configserver/configs
            - file:/Users/adrien/Documents/Informatique/formations/java-microservice/ecom-application/microservice/configserver/src/main/resources/configuration/ecom
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

management:
  endpoints:
    web:
      exposure:
        include: "busrefresh"

server:
  port: ${CONFIG_SERVER_PORT}

encrypt:
  key: ${CONFIG_SERVER_AES_KEY}

eureka:
  client:
    serviceUrl:
      defaultZone: "http://localhost:8761/eureka"
    register-with-eureka: true
    fetch-registry: true