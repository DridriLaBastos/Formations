info:
  name: Decrypt
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8888/decrypt
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
